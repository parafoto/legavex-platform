# LegasVex dependencies
# Install: pip install -r requirements.txt

# Telegram bot (Bot API polling — no webhook required)
# No external library needed for Bot API mode — uses stdlib urllib

# Telegram userbot (MTProto mode via Telethon)
telethon>=1.36.0

# Environment variables
python-dotenv>=1.0.0

# Configuration
pyyaml>=6.0

# PDF text extraction (document upload feature)
pypdf>=4.0.0

# TLS certificates (optional, improves SSL on some platforms)
certifi>=2024.1.0
